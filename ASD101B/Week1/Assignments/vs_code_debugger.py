


# Show the functionality of the debugger in VS Code

# This code will now run correctly in the debugger

def func():
    x = float(input('Enter a number : '))
    y = float(input('Enter a number : '))
    z = (x + y) / 2            
    print('The average of the two numbers you have entered is : ', z)

func()
